{
	"name": "lnpj-install",
	"description": "Install a default BriteSnow Linux Nginx/Node Postgres Java services.",

	"dependencies": {
		"cmdrouter": "^0.1.1",
		"fs-extra-plus": "^0.1.1",
		"p-spawn": "^0.2.0"
	},

	"scripts": {
	},

	"license": "MIT"
}